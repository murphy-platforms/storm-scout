# Storm Scout Deployment Configuration
# Copy this to .deploy.config.local and update with your values
# (don't commit .deploy.config.local - it's in .gitignore)

# Server connection
export DEPLOY_HOST="teammurphy.rocks"
export DEPLOY_USER="mwqtiakilx"
export DEPLOY_PORT="21098"

# Server paths
export DEPLOY_BACKEND_PATH="~/storm-scout"
export DEPLOY_FRONTEND_PATH="~/public_html"

# Optional: SSH key path (if not using default)
# export DEPLOY_SSH_KEY="~/.ssh/id_ed25519_prometric"
